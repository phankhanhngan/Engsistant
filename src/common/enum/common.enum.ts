export enum Role {
  TEACHER = 'TEACHER',
  STUDENT = 'STUDENT',
  ADMIN = 'ADMIN',
  UNKNOWN = 'UNKNOWN',
}

export enum ApiResponseStatus {
  SUCCESS = 'success',
  FAILURE = 'failure',
}

export enum LessonStatus {
  PENDING = 'PENDING',
  READY = 'READY',
}
